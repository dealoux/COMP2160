For the main.c, compile with DNDEBUG to disable the edge case testing, which will terminate the program

I can't really find a way to format the output (main1.c and main5.c) since the garbage collection statistic 
is printed everytime compact() is called as in the assignment description